<section class="intro-section">
  <img
    class="intro-art"
    src="/images/inkrain/rain-ink-hero.webp"
    alt="宣纸上的抽象雨痕与水墨"
    width="1920"
    height="1080"
    fetchpriority="high"
  />
  <div class="intro-copy">
    <p class="intro-kicker">癸巳年以来 · 写作与生活</p>
    <h1>听雨、写字、<br />悠哉悠哉</h1>
    <p class="intro-lead">
      这里收藏长文章、技术笔记，也收留那些尚未长成文章的片刻。纸面留白，墨色落字，朱砂只为重要之处。
    </p>
  </div>
  <div class="intro-inscription" aria-hidden="true">
    <span>一纸春秋 · 几行风雨</span>
    <b>墨雨</b>
  </div>
</section>

<style>
  .intro-section {
    position: relative;
    display: grid;
    grid-template-columns: minmax(0, 1fr) 180px;
    align-items: end;
    min-height: 490px;
    overflow: hidden;
    padding: 38px clamp(20px, 3.2vw, 36px) 56px;
    border-bottom: 1px solid var(--border);
  }

  .intro-art {
    position: absolute;
    inset: 0;
    z-index: -1;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center right;
    opacity: 0.68;
    mix-blend-mode: multiply;
    transition: opacity 180ms ease, filter 180ms ease;
  }

  :global(html[data-theme="moon"]) .intro-art {
    filter: grayscale(0.55) hue-rotate(145deg);
    opacity: 0.48;
  }

  :global(html[data-theme="ink"]) .intro-art {
    filter: invert(1) grayscale(1);
    mix-blend-mode: screen;
    opacity: 0.22;
  }

  .intro-copy {
    position: relative;
    z-index: 1;
    max-width: 660px;
  }

  .intro-kicker {
    margin: 0 0 18px;
    color: var(--ink-muted);
    font-family: var(--font-mono);
    font-size: 10px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  h1 {
    margin: 0 0 26px;
    color: var(--ink);
    font-family: var(--font-kai);
    font-size: clamp(3.45rem, 7.7vw, 6.4rem);
    font-weight: 400;
    line-height: 1.05;
    letter-spacing: 0;
  }

  .intro-lead {
    max-width: 560px;
    margin: 0;
    color: var(--ink-soft);
    font-size: 16px;
    line-height: 1.85;
  }

  .intro-inscription {
    position: relative;
    z-index: 1;
    align-self: stretch;
    border-left: 1px solid var(--border);
  }

  .intro-inscription span {
    position: absolute;
    top: 10px;
    left: 24px;
    color: var(--ink-muted);
    font-family: var(--font-serif);
    font-size: 12px;
    letter-spacing: 0.18em;
    writing-mode: vertical-rl;
  }

  .intro-inscription b {
    position: absolute;
    bottom: 0;
    left: 25px;
    display: grid;
    place-items: center;
    width: 34px;
    aspect-ratio: 1;
    color: var(--stamp);
    border: 1px solid currentColor;
    font-family: var(--font-kai);
    font-size: 13px;
    font-weight: 400;
  }

  @media (max-width: 760px) {
    .intro-section {
      grid-template-columns: minmax(0, 1fr);
      min-height: 520px;
      padding: 22px 10px 38px;
    }

    .intro-art {
      object-position: 62% center;
      opacity: 0.48;
    }

    h1 {
      font-size: clamp(3.15rem, 16vw, 5.1rem);
    }

    .intro-lead {
      max-width: 92%;
      font-size: 15px;
    }

    .intro-inscription {
      position: absolute;
      right: 10px;
      bottom: 30px;
      width: 36px;
      height: 118px;
      border-left: 0;
    }

    .intro-inscription span {
      display: none;
    }

    .intro-inscription b {
      right: 0;
      left: auto;
    }
  }
</style>
