---
import PageLayout from '../../../layouts/PageLayout.astro'
import PageMeta from '../../../components/PageMeta.astro'
import MomentsHero from '../../../components/MomentsHero.astro'
import TelegramList from '../../../components/TelegramList.astro'
import { SITE_TITLE } from '../../../config'
import { getChannelInfo } from '../../../lib/telegram'

const q = Astro.params.q || ''
const channel = await getChannelInfo(Astro, {
  q,
})

export const prerender = false
---

<PageLayout>
  <PageMeta title={`搜索碎碎念：${q} | ${SITE_TITLE}`} slot="meta" />
  <Fragment slot="main">
    <MomentsHero title={`搜索：${q}`} description="频道里的相关片段。" />
    <TelegramList channel={channel} />
  </Fragment>
</PageLayout>
